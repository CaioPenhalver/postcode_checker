<%= form_with model: @district, local: true do |form| %>
  <% if @district.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@district.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @district.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :district %><br>
    <%= form.text_field :name %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
<% end %>
